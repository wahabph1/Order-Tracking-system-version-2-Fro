{
  "buildCommand": "cd fronend/my-app && npm install && npm run build",
  "outputDirectory": "fronend/my-app/build",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
